# ピンメッセージ（仮称） — 仕様書 / プラン文書

> この文書の使い方: これは Claude Code に渡す「設計の確定事項」です。
> Claude Code には、まずこの仕様を読み込み、プランモードで実装計画を立ててから着手してもらいます。
> CLAUDE.md（リポジトリ内の振る舞いルール）は、scaffold 後に Claude Code 自身に書かせます。
> その種になる「働き方の約束」は本文末尾にあります。

---

## 1. 目的（何を・なぜ作るか）

特定の場所にテキストメッセージを「置いて」おき、**その場所に物理的に行った人だけがメッセージを開ける**ウェブアプリ。
位置情報メッセージ／デジタル宝探しのジャンル。少人数で使う PoC として作る。

体験のコア:
- 投稿者は地図上の任意の場所にピンを立て、ひとことメッセージを添えて置く。
- ピンの場所は地図上に見える（隠さない）。「いつかそこへ行ったら開こう」という気持ちを誘発するため。
- 受信者はそのピンの場所に実際に行くと、GPS で近接が検知され、ロックが外れてメッセージを読める。

---

## 2. スコープ（今回の PoC で作るもの）

- ユーザーは 2 人。**認証なし**（ログイン機能を作らない）。
- ピンの場所は地図に**常に表示**する（近づくまで隠す方式は採らない）。
- ロック解除は **距離判定のみ**。**いったん場所を離れたら再びロックされる**方式から始める（実装が最もシンプルなため。理由は §6）。
- 画面は2種類: ①地図 UI、②メッセージ確認モーダル。

## 2.1 やらないこと（Non-Goals / 重要）

Claude Code はここを勝手に作り込まないこと:

- ユーザー認証・ログイン・サインアップ
- 「一度開けたら永久に読める」永続化（v2 で localStorage を使って追加する想定。§10）
- サーバ側での距離判定 / PostGIS（v2 候補。§10）
- React 化（v2 で実施。§10）
- 通知・友達機能・複数ルーム・画像添付などの追加機能
- 早すぎる抽象化（PoC に不要な汎用化・状態管理ライブラリ導入など）

---

## 3. 技術選定と理由

| 領域 | 採用 | 理由 |
|------|------|------|
| 言語/構成 | **vanilla JS（ESモジュール）+ Vite** | 今週の新規要素（地図・GPS・リアルタイム・AI駆動フロー）に集中するため。Leaflet が命令型ライブラリで、地図・GPS・購読を全て同じ命令型・イベント駆動で書けて頭のモードを切り替えずに済む。React は v2 に回す。 |
| 地図 | **Leaflet + OpenStreetMap** | APIキー不要・課金不要・カード登録不要で完全無料。CDN/npm で入る。クリックでピンを立てれば緯度経度が手に入り、ジオコーディング等の有料 API が一切絡まない。Google Maps は無料枠内でも billing 有効化（カード登録）が必須なので回避。 |
| BaaS | **Supabase（Postgres + Realtime）** | 使い慣れている。npm で `@supabase/supabase-js` を入れて import する形なので Vite と素直に噛み合う。`created_at` の自動付与など Postgres の利点も得られる。 |
| ホスティング | 静的ホスティング（GitHub Pages / Vercel / Netlify のいずれか） | Vite を build して配信。**Geolocation は HTTPS 必須**だが、これらは HTTPS を提供するので問題なし。localhost も許可される。 |

依存追加は上記スタックに限定。これ以外のライブラリを入れたくなったら、入れる前に必ず相談すること。

---

## 4. データモデル（Supabase）

テーブル `messages`:

| カラム | 型 | 備考 |
|--------|------|------|
| `id` | `uuid`（default `gen_random_uuid()`）または `bigint` 主キー | |
| `user_name` | `text` | 投稿者名 |
| `text` | `text` | ひとことメッセージ |
| `lat` | `float8` | ピンの緯度（地図クリックで取得） |
| `lng` | `float8` | ピンの経度（地図クリックで取得） |
| `created_at` | `timestamptz` default `now()` | Postgres が自動付与 |

> ロックの開閉状態はこのテーブルに**持たせない**（理由は §6）。

---

## 5. ユーザーフロー（2つの振る舞い）

利用者は同じ1つのアプリを開く。役割でアプリを分けない。状況に応じて投稿/閲覧を行う。

### 投稿（送信者としての振る舞い）
1. 地図を表示
2. 地図をクリック → その地点の緯度経度を取得し、仮ピンを立てる
3. モーダルで名前とメッセージを入力
4. `messages` に INSERT して保存（Supabase）

### 閲覧（受信者としての振る舞い）
1. 地図を表示
2. `messages` を SELECT で全件取得 → 地図にマーカー表示
3. Realtime 購読で、以後に追加されたピンも即座にマーカー追加
4. 自分の現在地を Geolocation で取得し、現在地マーカーを表示
5. マーカーをタップ → モーダルを開く。現在地とそのピンの距離を計算し、
   - **しきい値以内** → メッセージ本文を表示
   - **遠い** → ロック表示（例: 「📍 ここから約 80m 先にメッセージがあります」）

---

## 6. 重要原則

### 原則1: ロック状態は保存しない（その場で計算する派生情報）
ピンが「開けるか/ロックか」は受信者の現在地に依存し、移動するたびに変わる**ライブな値**。
DB やどこかに保存せず、`distance(自分の現在地, ピンの座標) < しきい値` を**毎回その場で計算**して決める。

```
unlocked = distance(現在地, ピン) < THRESHOLD   // 近ければ true、離れれば false
```

- 保存するデータ = ピンの中身（`user_name / text / lat / lng`）。全員共通。
- 計算で出すデータ = 開けるか否か。人ごと・移動ごとに変わる。

### 原則2: このアプリの骨格は「習ったチャット」
チャットの 1 件 `{ userName, text }` に座標を足しただけ。
保存（INSERT）と受信（Realtime 購読）の型はチャットと同じ。**唯一変わるのは「受信したデータの表示先がリストから地図に変わる」点だけ**。

### 原則3: まず再ロック方式で作る
「離れたら再ロック」は記録不要で 1 行の距離計算だけで済む。これが原則1の最も素直な実装。
「永久に見られる」は「過去にそこへ行った事実」を別途記録する一手間が増えるので v2（§10）に回す。

---

## 7. 実装メモ（新規要素 3 つ）

### 7.1 Geolocation（現在地の取得）
- `navigator.geolocation.getCurrentPosition(success, error, options)` を使う。
- **コールバック型**。クリックや Realtime 購読と同じイベント駆動の仲間。
- **HTTPS 必須**（localhost は可）。許可ダイアログが出る。過去の `getUserMedia`（カメラ許可）と同じ性質。
- 連続追従が要るなら `watchPosition` も検討（PoC では `getCurrentPosition` で開始 → 必要なら拡張）。

### 7.2 距離判定（ハバーサイン）
- 2 つの緯度経度から距離を求める関数を 1 個用意して使い回す（ハバーサインの式）。
- しきい値 `THRESHOLD` は **30〜50m** から開始（GPS 誤差 5〜20m を吸収するため。狭すぎると正しい場所でも開かない事故になる）。**現地で調整する前提の暫定値**。

### 7.3 Supabase の Realtime と RLS（ここが要注意）
- Realtime 購読は `supabase.channel(...).on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'messages' }, cb).subscribe()` の形。`onChildAdded` がこれに置き換わる。
- 対象テーブルを **Realtime の publication に追加**（ダッシュボードで有効化）する必要がある。
- **RLS（行レベルセキュリティ）の落とし穴**:
  - RLS 未設定だと anon キーで誰でも全データ読み書きできてしまう（本来は危険）。
  - 逆に RLS を有効にしてポリシー未設定だと、読み取りが空・挿入が失敗して「なぜ動かない？」になる（初心者の鉄板トラップ）。
  - **Firebase のテストモード相当の初期設定**: RLS を有効化し、anon に SELECT と INSERT を許す緩いポリシーを置く（PoC 用。本番では適切に絞ること）。
  - **重要**: Realtime 購読は RLS に従うため、**SELECT ポリシーが届けたい行をカバーしていないとリアルタイムが飛んでこない**。「INSERT は成功するのに相手の画面に出ない」症状はだいたいこれ。
- 鍵: 従来の anon キーは 2026 年末まで有効。新しい publishable キー（`sb_publishable_xxx`）が推奨。PoC はどちらでも可。

PoC 用 RLS ポリシー例（参考。Claude Code に正確な手順は確認させること）:
```sql
alter table messages enable row level security;

create policy "anon can read"   on messages for select to anon using (true);
create policy "anon can insert" on messages for insert to anon with check (true);
```

---

## 8. 画面

- **地図 UI**: Leaflet の地図。ピンのマーカー群＋自分の現在地マーカー。
- **メッセージ確認モーダル**:
  - 投稿時 = 名前・メッセージ入力フォーム。
  - 閲覧時 = 近ければ本文、遠ければロック表示（距離の目安を出す）。

---

## 9. 1 週間の組み立て（ビルド順）

各日「動くものを確認してから次へ」を守る。

- **Day 1 — 土台**: Vite(vanilla) で scaffold。Supabase プロジェクト作成 → `messages` テーブル → RLS ポリシー → Realtime 有効化。`@supabase/supabase-js` 接続。キーを env に。接続確認。
- **Day 2 — 地図**: Leaflet で地図表示。クリックで緯度経度取得し仮マーカー表示（保存はまだ）。
- **Day 3 — 投稿**: クリック → モーダルで入力 → Supabase に INSERT。ダッシュボードで行が増えるのを確認。
- **Day 4 — 表示と同期**: 全件 SELECT して地図にマーカー描画。Realtime 購読で新規ピンが即反映。**2 つのブラウザ（できればスマホ）でテスト**。
- **Day 5 — GPS とロック**: 現在地取得 → 現在地マーカー。ハバーサインで距離計算。マーカータップで近ければ本文・遠ければロックのモーダル分岐。
- **Day 6 — 仕上げ + デプロイ**: ロック/解除の UX、距離の目安表示、しきい値付近のチラつき調整。HTTPS にデプロイ。
- **Day 7 — 予備 + 実地テスト**: 現地で実機の動作確認。余力あれば §10 の永続化に着手。

---

## 10. 既知の注意点と将来（v2）

### 既知の注意点（PoC 段階では深追いしない）
- **GPS のチラつき**: しきい値付近で開いたり閉じたりする。実装の複雑さではなく UX の問題。後から「一度開いたら少し余裕を持たせる」等で滑らかにできる。
- **RLS で読めない/届かない**: §7.3 を参照。最初に握っておく。
- **Geolocation 精度**: スマホ屋外で 5〜20m、PC（WiFi）だと 100m 超もある。受信者は現地でスマホを使う想定なので精度の良い側に出る。

### v2（今回はやらない・将来の伸ばしどころ）
- **「一度開けたら永久に見られる」**: 「過去にそこへ行った事実」を **localStorage** に記録（メモアプリで使った保存→読み出しがそのまま使える。新技術ゼロのご褒美ステップ）。ライブな状態は計算のまま、変わらない過去の事実だけ保存。
- **React 化**: ドメインロジックは理解済みなので、新しい挑戦は「Leaflet を React にどう乗せるか」だけに絞れる。
- **PostGIS で距離判定をサーバ側へ**: Supabase は Postgres なので `geography(Point)` + `ST_DWithin` 等が使える。

---

## 11. Claude Code への働き方の約束（CLAUDE.md の種）

> Claude Code へ: 以下を CLAUDE.md に取り込み、毎セッション守ってください。

- 自分（開発者）は**学習中**。コードを一気に吐かず、**判断の理由を説明しながら**進めること。
- **小さく・レビュー可能な単位**で進める。1 ステップごとに止めて確認を取る。
- 学習機会のある箇所は、可能なら**自分に書かせてレビューする**スタイルを優先（完成コードの丸投げより、ヒントと Q&A）。
- **jQuery は使わない**。vanilla JS（ES モジュール）で書く。
- **過剰実装しない**。これは 2 人・認証なしの PoC。§2.1 の Non-Goals を勝手に作らない。
- **新しい依存を入れる前に相談**する（§3 のスタック以外）。
- 既存ファイルの大規模リファクタや、認証・永続化・React 化を勝手に始めない。必要だと思ったら提案して確認を取る。
- API キー等の秘密情報をコードに直書きしない。env で扱う。
